module instruction_memory(
  input [15:0] code_in,
  input [11:0] addr, 
  input we, 
  input clk, 
  output [15:0] q
);
  
  reg [15:0] ram [0:4095];
 
  
  always @ (posedge clk)
    begin
      if(we)
        ram[addr] <= code_in;
    end
 
  assign q = ram[addr];
  
endmodule